name: Update live tv Playlist

on:
  # Her 6 saatte bir çalışır (Her 1 saat için: '0 * * * *')
  schedule:
    - cron: '0 */6 * * *'

  # Manuel tetikleme
  workflow_dispatch:

  # Kod değişikliğinde tetikleme
  push:
    branches:
      - main
    paths:
      - 'livetv.py'

concurrency:
  group: update-m3u
  cancel-in-progress: true

permissions:
  contents: write

jobs:
  update-playlist:
    runs-on: ubuntu-22.04
    timeout-minutes: 15

    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v4

      - name: 🐍 Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: 📦 Install dependencies
        run: |
          pip install requests playwright
          playwright install --with-deps chromium

      - name: 🔍 Run scraper
        run: python livetv.py
        env:
          PYTHONUNBUFFERED: "1"

      - name: 📤 Commit and push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add -f livelive24.m3u
          if git diff --cached --quiet; then
            echo "Değişiklik yok"
          else
            TIMESTAMP=$(date -u '+%Y-%m-%d %H:%M UTC')
            git commit -m "🔄 Update playlist | ${TIMESTAMP}"
            git push
          fi
